<div class="row">
    <div class="col-8">
      <h2>Lista de Categorias:</h2>
    </div>
    <div class="col-4 text-right">
      <a class="pt-1" style="display: inline-block" href="/admin/categorias/add"><button class="btn btn-primary">Nova Categoria</button></a>
    </div>
  </div>
<hr>
{{#each categorias}}
<!--Card de Cada Categoria-->
<div class="card mt-2">
  <div class="card-body">
    <h5 class="card-title">{{nome}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">
        <div class="row">
            <div class="col-8 text-left">
            Criado: {{date}} 
            </div>
            <div class="col-4 text-right">
            Slug: {{slug}}
            </div>

        </div>
        
        
    </h6>
    <div class="row">
        <div class="col">
            <a href="/admin/categorias/edit/{{id}}" class=""><button class="btn btn-primary">Editar</button></a>

            <button type="button" data-toggle="modal" data-target="#DeleteModal{{id}}"
            class="btn btn-primary">Deletar</button>

        </div>
        

    </div>
    
  </div>
</div>

<!--Modal do Delete-->
<div class="modal fade" id="DeleteModal{{id}}" tabindex="-1" role="dialog" aria-labelledby="DeleteModalLabel{{id}}"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="DeleteModalLabel{{id}}">Atenção</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Tem certeza que deseja deletar essa categoria? essa ação é <center><b>IRREVERSÍVEL</b></center>
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Não, volte.</button>
                <form action="/admin/categorias/deletar" method="POST">
                    <input type="hidden" name="id" value="{{id}}">
                    <button type="submit" class="btn btn-danger">Sim, Delete.</button>
                </form>
                
            </div>
        </div>
    </div>
</div>

{{else}}

<div class="row my-5">
    <div class="col text-center">

    <h4>Nenhuma categoria registrada.</h4>

    </div>
</div>

{{/each}}
