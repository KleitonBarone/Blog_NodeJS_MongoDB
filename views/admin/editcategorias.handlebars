<div class="card">
  <div class="card-body">

    <h2>Editar Categoria:</h2>
    <hr>
    {{#each erros}}

    <div class="alert alert-danger alert-dismissible fade show" role="alert">
      {{texto}}
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    {{else}}

    {{/each}}
    <form action="/admin/categorias/edit" method="POST">
      <input type="hidden" name="id" value="{{categoria.id}}">
      <div class="form-group">
        <label for="nome">Nome</label>
        <input name="nome" type="text" class="form-control" id="nome" placeholder="Nome da categoria" value="{{categoria.nome}}">
      </div>

      <div class="form-group">
        <label for="slug">Slug</label>
        <input type="text" class="form-control" id="slug" name="slug" placeholder="Slug da categoria" aria-describedby="slughelp" value="{{categoria.slug}}">
        <small id="slughelp" class="form-text text-muted">
          Slug é como vai ficar a categoria na url. ex: www.site.com/categoria/acao. Nesse caso o Slug é acao.
        </small>
      </div>

      <button type="submit" class="btn btn-primary">Editar</button>

    </form>
  </div>
</div>